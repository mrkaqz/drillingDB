services:
  app:
    image: ghcr.io/mrkaqz/drillingdb:latest
    ports:
      - "7000:7000"
    environment:
      - DATA_DIR=/data
    volumes:
      - drilling_data:/data
    restart: unless-stopped

volumes:
  drilling_data:  # contains db/, uploads/, and exports/ subdirectories
